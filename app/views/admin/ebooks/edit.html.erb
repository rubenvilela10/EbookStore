<% content_for :title, "Editing ebook" %>
<% flash.each do |type, message| %>
  <p class="mb-4 p-3 rounded 
            <%= type == "alert" ? "bg-red-100 text-red-700" : "bg-green-100 text-green-700" %>">
    <%= message %>
  </p>
<% end %>

<div class="form-page">
  <h1>Editing ebook</h1>
  <%= render "form", ebook: @ebook %>
</div>
